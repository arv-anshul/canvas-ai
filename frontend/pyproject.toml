[project]
name = "frontend"
requires-python = ">=3.11"
dynamic = ["version"]
dependencies = [
    "httpx>=0.27.2",
    "streamlit-drawable-canvas>=0.9.3",
    "streamlit>=1.38.0",
]

[tool.ruff]
extend = "../pyproject.toml"
